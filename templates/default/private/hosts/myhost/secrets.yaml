# Placeholder — NOT encrypted yet. After setting your key in ../../.sops.yaml, encrypt in place:
#   sops --encrypt --in-place hosts/myhost/secrets.yaml
# Keys must match what hosts/myhost/secrets.nix references.
cloudflare-dns-token: REPLACE_ME
smtp-password: REPLACE_ME
